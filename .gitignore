# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project
.firecrawl/
discord.env
discord_token.json
target/
src-tauri/target/
apex-native/target/

# Agent worktrees (Claude Code session isolation)
.claude/worktrees/
.playwright-mcp/


# Added by cargo

/target

# Locally-generated sidecar binary placeholders (per-platform build artifacts).
# Agents touch these as zero-byte stubs so the Tauri build script does not
# fail; the real binaries are produced by CI. The Windows binary IS tracked
# (per Wave 7B notes) and intentionally not listed here.
src-tauri/binaries/ococo-api-aarch64-apple-darwin
src-tauri/binaries/ococo-api-x86_64-unknown-linux-gnu

# dev-inspector generated artifacts
dev/bug_report.json
dev/bug_report.md
