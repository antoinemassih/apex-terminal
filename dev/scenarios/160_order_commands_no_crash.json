{
  "name": "order_commands_no_crash",
  "description": "CancelAllOrders, ClearOrderHistory, PlaceAllDraftOrders, PlaceAllDraftAlerts have no headless state mutation but must not crash. Zero coverage before this scenario.",
  "story": "UserJourney/OrderEntry",
  "tags": ["orders", "smoke", "robustness"],
  "settle_ms": 0,
  "abort_on_failure": false,
  "steps": [
    { "action": "reset" },
    { "action": "wait_frames", "count": 2 },
    { "action": "log", "message": "Firing all order commands — none mutate headless state" },
    { "action": "cmd", "cmd": "CancelAllOrders", "args": {} },
    { "action": "cmd", "cmd": "ClearOrderHistory", "args": {} },
    { "action": "cmd", "cmd": "PlaceAllDraftOrders", "args": {} },
    { "action": "cmd", "cmd": "PlaceAllDraftAlerts", "args": {} },
    { "action": "wait_frames", "count": 2 },
    { "action": "assert", "assertions": [
      { "fps_above": 5.0 },
      { "no_active_violations": true },
      { "pane_count_equals": 1 }
    ]},
    { "action": "log", "message": "Order commands fired — app alive, FPS stable" }
  ]
}
