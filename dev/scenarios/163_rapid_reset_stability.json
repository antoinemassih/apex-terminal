{
  "name": "rapid_reset_stability",
  "description": "Fire reset 5 times in quick succession — verifies the reset mechanism is idempotent and doesn't leave the app in a partially-reset state. After the last reset, standard initial assertions must pass.",
  "story": "Baseline/Health",
  "tags": ["reset", "stability", "stress"],
  "settle_ms": 0,
  "abort_on_failure": false,
  "steps": [
    { "action": "log", "message": "Firing 5 rapid resets" },
    { "action": "reset" },
    { "action": "reset" },
    { "action": "reset" },
    { "action": "reset" },
    { "action": "reset" },
    { "action": "wait_frames", "count": 3 },
    { "action": "assert", "assertions": [
      { "pane_count_equals": 1 },
      { "indicator_count_equals": 0 },
      { "watchlist_section_count_equals": 1 },
      { "no_open_dialogs": true },
      { "fps_above": 5.0 }
    ]},
    { "action": "log", "message": "After 5 resets: state clean, FPS stable" }
  ]
}
